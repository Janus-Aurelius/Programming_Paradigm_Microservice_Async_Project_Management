package com.pm.commoncontracts.domain;

public enum ProjectStatus {
    PLANNING,
    PENDING,
    IN_PROGRESS,
    COMPLETED,
    FAILED,
    CANCELLED

    // Optional: You can add methods here if needed
}
